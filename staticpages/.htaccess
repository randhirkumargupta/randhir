<IfModule mod_rewrite.c>
        RewriteEngine On
	RewriteCond %{QUERY_STRING} ^(.*)=(.*)$
        RewriteRule ^(.*)?(.*)$ /$1-@@-%0? [R=301,L]
</IfModule>
<IfModule mod_rewrite.c>
        RewriteEngine On
	RewriteRule ^(.*):(.*)$ /$1@--@$2 [L,R=301]
</IfModule>
